import { Meta, Story, Canvas } from '@storybook/addon-docs';
import {Confrim_Modal} from "./index.js";
import {Alert_Modal} from "./index.js";

<Meta title="Modal"  component="Modal" />

# Modals

With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.

/ Principles 
/ Types 
/ Specs
/ Behavies or states

## I. Alert Modal - Design / / Usage // Specs

With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.

## I. Alert Modal - Wireframe

With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.

## I. Alert Modal - Anatomy 

With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.

## I. Alert Modal - Coded example 

With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.

<Canvas>
  <Story name="Alert Modal">
      <Alert_Modal />
  </Story>
</Canvas>

### --

## II. Confirmation Modal - Design / Usage 

With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.

## II. Confirmation Modal - Wireframe

With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.

## II. Confirmation Modal - Anatomy 

With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.

## II. Confirmation Modal - Coded example 

With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.

<Canvas>
  <Story name="Confrim_Modal">
    <Confrim_Modal />
  </Story>
</Canvas>