import { Meta, Story, Canvas } from '@storybook/addon-docs';
import {MenuTabs} from "./index.js";
import {HiddenMenu} from "./index.js";
import {BottomNavigation} from "./index.js";

<Meta title="Navigation"  component="Navigation" />

# Navigation Components

With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.

## Navigation - Menu Tabs

With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.

<Canvas>
  <Story name="MenuTabs">
      <MenuTabs />
  </Story>
</Canvas>

## Navigation - Modal drawer

With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.

<Canvas>
  <Story name="HiddenMenu">
      <HiddenMenu />
  </Story>
</Canvas>

## Navigation - Bottom drawer

With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.

<Canvas>
  <Story name="BottomNavigation">
      <BottomNavigation />
  </Story>
</Canvas>